# v1.7.x User-Visible Delta

This support-task delta extends Issue #157. It records the verifier, CLI,
and schema-version surface that became visible in the v1.7.x train without
replacing the product work tracked in the source issues.

## Changes Since #157

- Reason-code taxonomy reference: `docs/errors.md` now documents the SDK-public
  `att.verify.*` rejection reason surface introduced by Issue #172.
- `verify --explain`: adds a human-oriented explanation path for rejected
  verifier results so operators can read the failure without parsing the JSON
  payload by hand.
- `verify --json`: emits the machine-readable CLI report envelope, including
  `schema_version`, `bundle_schema_version`, `ok`, `reasons[]`, and
  `verifier_version`.
- `schema_version` policy: the CLI report schema is independent from the
  bundle schema version, and unsupported bundle versions should surface in
  `reasons[]` with an unsupported-version code.
- negative conformance vectors: see `tests/conformance/vectors/negative/`
  for the failure cases that pin required-field, signature-missing, and
  schema-version behavior.

## CI Gating Example

Use the exit code as the gate and inspect `reasons[]` from the JSON payload:

```sh
attestplane verify "$bundle" --json > verify.json
rc=$?

if [ "$rc" -ne 0 ]; then
  jq -r '.reasons[] | [.code, .field, .message] | @tsv' verify.json
  exit "$rc"
fi
```

In this pattern, `reasons[]` is the machine-readable verifier rejection list.
It is empty when `ok=true`.

## Cross-References

- [`docs/errors.md`](../errors.md) - stable verifier error taxonomy and the
  `att.verify.*` reason-code table.
- [`docs/cli/verify-json.md`](../cli/verify-json.md) - consumer contract for
  `verify --json`.
- [`docs/schema/verify-json.md`](../schema/verify-json.md) -
  `schema_version` policy note for verifier JSON consumers.
