# v1.8.19

`v1.8.19` is an automated release from autodev-train.

## Changes
- 19b69c8 chore(release): bump version to 1.8.19 [skip ci]
- P1 conformance tightening: pin the `verify --json` output contract to the
  versioned golden fixture, split quarantine versus usage-error exit codes, and
  reject unknown required `schema_version` fields with the stable quarantine
  code path.
