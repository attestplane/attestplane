# v1.8.7

`v1.8.7` is an automated patch release from autodev-train (Loop 2 → Loop 3 transition).

## Changes Since v1.8.6

- fix(autodev): handle CI failures and REQUEST_CHANGES PRs gracefully
- fix(architecture-audit): upgrade actions permission to write for dispatch
- fix(auto-loop): use git log %ct epoch to avoid timezone comparison bugs
- fix(auto-loop): bump package versions before tagging for release-cd compatibility
- fix(autodev): run ruff --fix + format after Codex to reduce CI failures
- fix(autodev): switch merge CI gate from broken `--json required` to mergeStateStatus

## Notes

Loop 2 produced no merged PRs (all failed CI); auto-loop advanced via the
`closed_failed >= 1 && prs_since == 0` fallback. Loop 3 started with
architecture-audit v1.8.7 targeting FreeTSA anchoring, taxonomy pinning,
and exit-code contract improvements.
- `attestplane verify --json` now distinguishes verification failures (`2`),
  schema/output-contract failures (`3`), and quarantined/anchor-unverifiable
  bundles (`4`) in the documented CI-gating contract.
