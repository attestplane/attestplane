# v0.0.3-alpha Release Asset Upload Report

- **Date**: 2026-05-18
- **Upload timestamp**: 2026-05-18T07:04:43Z (UTC)
- **Verdict**: `V003AlphaReleaseAssetUploadComplete`
- **Main commit**: `d2b90a53005ed98b3374883d5b394e61664bd718`
- **Frozen v0.0.3-alpha tag target**: `9bde6338df008afe58d561b0ba66eaaf75e298ad` (NOT moved)
- **GitHub Release URL**:
  <https://github.com/attestplane/attestplane/releases/tag/v0.0.3-alpha>

## Executive Summary

Founder-authorized upload of 5 release assets to the existing
`v0.0.3-alpha` GitHub Release. The Release remained a prerelease;
the annotated tag was not moved; PyPI / npm registries were not
re-published; the npm `latest` dist-tag was not modified. The upload
follows the P3.3 dry-run plan exactly and was preceded by a fresh
P3.3 gate run that verified manifest ↔ artefact SHA-256 consistency.

## Uploaded Assets (5)

| Name | Size (bytes) | Content-Type | SHA-256 |
|---|---|---|---|
| `attestplane-0.0.3a0-py3-none-any.whl` | 153,372 | application/octet-stream | `623cd39e9ac4819f3a88a78715c4da7426882a371635c0c899249608a1d78e7d` |
| `attestplane-0.0.3a0.tar.gz` | 207,215 | application/x-gtar | `e37a78d0252eae0d831b9740dfce40d3972484ad47700070e907e2cfa74ff2db` |
| `attestplane-attestplane-0.0.3-alpha.tgz` | 110,826 | application/x-gtar | `471d37afcd2c1cf1038de5d860df54bd8ea55e72f935a713562de718901c9e53` |
| `checksums.sha256` | 961 | application/octet-stream | (text manifest of the three SHA-256 above) |
| `artifact-manifest.json` | 4,731 | application/json | (P3.3 dry-run artefact manifest, schema v1) |

Asset count: **5**

Each asset's `state` is `uploaded` per `gh release view v0.0.3-alpha`.

## Pre-Upload Gates Passed (in order)

1. Working tree clean ✓
2. Branch is `main` ✓
3. `main` == `origin/main` == `d2b90a53…` ✓
4. `v0.0.3-alpha^{}` == `9bde6338…` (frozen) ✓
5. `gh release view v0.0.3-alpha` returned `isPrerelease: true`, `assets: []` ✓
6. PyPI `attestplane==0.0.3a0` HTTP 200 (unchanged) ✓
7. npm `@attestplane/attestplane@0.0.3-alpha` present, `latest` still
   `0.0.1-alpha.1` (untouched) ✓
8. Python `python -m build` reproduced wheel + sdist; `twine check` PASSED
   for both ✓
9. TypeScript `npm ci` + `npm run lint` + `npm test` + `npm run build` +
   `npm pack` reproduced the 110.8 kB tarball ✓
10. `scripts/check-release-assets-dry-run.sh`: PASS
    - hygiene scan: clean
    - SHA-256 manifest ⇆ on-disk: all 3 match
    - `checksums.sha256` line consistency: all 3 verified
    - upload-plan execution check: assets `[]` pre-upload
    - tag freeze: `9bde6338…` (frozen)
    - claim scan: clean
    - jq empty: PASS
    - git diff --check: clean

## Upload Command Executed

```bash
gh release upload v0.0.3-alpha \
  sdk/python/dist/attestplane-0.0.3a0.tar.gz \
  sdk/python/dist/attestplane-0.0.3a0-py3-none-any.whl \
  sdk/typescript/attestplane-attestplane-0.0.3-alpha.tgz \
  release/artifacts/v0.0.3-alpha/checksums.sha256 \
  release/artifacts/v0.0.3-alpha/artifact-manifest.json
```

No `--clobber` flag (Release assets were `[]` before the upload, so the
default no-overwrite semantics is the safe choice).

## Hygiene Scan Result (pre-upload)

- Patterns scanned: `.env`, `credentials`, `token`, `secret`,
  `node_modules`, `__pycache__`, `.DS_Store`, `.git`, `private_key`,
  `id_rsa`, `id_ed25519`, `pypirc`, `.npmrc`
- Files scanned across 3 artefacts: 295 entries (sdist 128 + wheel 54 +
  npm tarball 113)
- Result: **clean**

## Release Surface Invariants (verified after upload)

- `v0.0.3-alpha^{}` == `9bde6338df008afe58d561b0ba66eaaf75e298ad` — tag
  NOT moved.
- GitHub Release `v0.0.3-alpha`:
  - `isPrerelease: true` (unchanged)
  - `tagName: v0.0.3-alpha` (unchanged)
  - `assets`: 5 entries (the new upload)
- GitHub Release title, body/notes, prerelease flag: not modified by
  this upload (only the assets list changed).
- PyPI `attestplane==0.0.3a0`: HTTP 200 (no re-publish).
- npm `@attestplane/attestplane@0.0.3-alpha`: present (no re-publish).
- npm dist-tags: `latest: 0.0.1-alpha.1` (NOT modified),
  `alpha: 0.0.3-alpha` (unchanged).
- No publish workflow triggered.
- No tag movement, no retag, no force push.

## Safe Claims

- v0.0.3-alpha GitHub Release now carries 5 audit-friendly assets
  uploaded by the founder-authorized release steward, traceable to
  `main@d2b90a53…` and tag `9bde6338…`.
- Asset SHA-256 values are byte-identical to the values in
  `release/artifacts/v0.0.3-alpha/checksums.sha256` and
  `release/artifacts/v0.0.3-alpha/artifact-manifest.json` committed in
  this branch.
- Hygiene scan on the artefact bodies returned clean before upload.
- The Release continues to be a prerelease; downstream consumers should
  read it as alpha-stage evidence, not as a production artefact.

## No-Go Claims (preserved post-upload)

- The upload does NOT make v0.0.3-alpha production-ready.
- The upload does NOT make v0.0.3-alpha compliance-ready.
- The upload does NOT make v0.0.3-alpha certification-ready.
- The upload does NOT constitute SLSA Level 3 attestation.
- The upload does NOT constitute certified provenance.
- The upload is NOT production-grade supply-chain security.
- SHA-256 checksums are integrity metadata, NOT cryptographic signing;
  they do NOT provide non-repudiation.
- The uploaded `artifact-manifest.json` is documentation, NOT a legal
  or compliance attestation.
- The uploaded `attestplane-attestplane-0.0.3-alpha.tgz` is the same
  tarball already on npm under the `alpha` dist-tag; the GitHub
  Release copy is a convenience mirror, NOT a separately attested
  artefact.
- The uploaded Python wheel + sdist are local-built copies under
  `main@d2b90a53…`; they are NOT byte-identical to the registry
  artefacts on PyPI which were built from `main@51c4e6a…`. This is
  documented in `artifact-manifest.json` under
  `artifacts[].registry_status`.

## Remaining Limitations

- No cosign / Sigstore signature on the uploaded assets in this round.
- No SLSA L3 provenance attestation generated for the uploaded assets.
- Local-built copies on GitHub Release are NOT bit-identical to PyPI /
  npm registry artefacts; downstream consumers seeking the canonical
  registry artefact should fetch from PyPI / npm directly, not from
  the GitHub Release mirror.
- npm `latest` dist-tag promotion is deferred; `alpha` remains the
  only dist-tag pointing at `0.0.3-alpha`.

## Companion Files (committed earlier)

- `release/artifacts/v0.0.3-alpha/checksums.sha256`
- `release/artifacts/v0.0.3-alpha/artifact-manifest.json`
- `release/artifacts/v0.0.3-alpha/upload-plan.md`
- `scripts/check-release-assets-dry-run.sh`
- `docs/validation/p3_3_release_assets_checksums_report.{md,json}`

## Companion JSON

`docs/validation/v0.0.3_alpha_release_asset_upload_report_20260518.json`
