# v0.0.3-alpha Post-tag Release Surface Audit

## Executive Summary

Verdict: `V003AlphaReleaseSurfaceFrozen`.

The v0.0.3-alpha release surface is frozen for an alpha hardening release. The pushed annotated tag points to the intended manifest commit, the manifest commit is on `main` and `origin/main`, and the release draft remains alpha-scoped with explicit no-go claims.

No GitHub Release was created during this audit. No release assets were uploaded. No PyPI or npm package was published.

## Repository State

- Current branch: `main`
- Local `main` at audit start: `9bde6338df008afe58d561b0ba66eaaf75e298ad`
- `origin/main` at audit start: `9bde6338df008afe58d561b0ba66eaaf75e298ad`
- Annotated tag object: `addd2f318dc4e43509ceea6750ceda61d17b5ca7`
- Peeled tag target: `9bde6338df008afe58d561b0ba66eaaf75e298ad`
- Release-content merge commit: `be5383d9d555b5b1eac437efc8c8a4201a028cab`
- Manifest commit: `9bde6338df008afe58d561b0ba66eaaf75e298ad`
- Remote tag `origin/v0.0.3-alpha`: pushed and resolves to the same peeled target.
- Remote `origin/main`: pushed and equals the manifest commit at audit start.

Note: this post-tag audit report is intentionally committed after the tag and is not part of the `v0.0.3-alpha` tag contents.

## Pushed Remote Status

Confirmed:

- `git ls-remote --tags origin "v0.0.3-alpha*"` reports the annotated tag object and peeled target.
- `git ls-remote origin main` reports `9bde6338df008afe58d561b0ba66eaaf75e298ad`.
- `main` and `origin/main` matched before this post-tag audit commit.

## Passed Gates Summary

The release operator stage had already passed:

- `scripts/check-storage-compatibility.sh`
- `scripts/check-release-provenance.sh`
- `scripts/check-fault-injection.sh`
- `scripts/check-public-api.sh`
- `bash scripts/check-policy.sh`
- `jq empty docs/validation/*.json api/public/*.json storage/compat/*.json release/artifacts/*.json tests/fault_injection/*.json`
- `git diff --check`
- Quick Python release/storage/provenance tests: 25 passed.
- `cd sdk/typescript && npm run lint`

This post-tag audit rechecked:

- Git branch, tag, remote tag, and remote `main` state.
- Draft release notes surface.
- Tag-contained evidence files.
- v0.0.3 artifact manifest fields.
- GitHub Release absence via `gh release view v0.0.3-alpha`.

## Release Draft Claim Audit

Audited:

- `docs/validation/v0.0.3_alpha_github_release_draft_20260518.md`

Result: PASS. No draft edits were required.

The draft states:

- `Status: alpha hardening release`
- CLI `attestplane verify` remains `chain_report_only`.
- The release is not production-ready, not compliance-ready, and not a certification system.
- The draft includes safe claims and no-go claims as explicit sections.

Forbidden-phrase search hits were limited to negative, no-go, or not-in-scope contexts.

## Tag Evidence Audit

The `v0.0.3-alpha` tag contains:

- `docs/release-notes/v0.0.3-alpha.draft.md`
- `docs/validation/v0.0.3_alpha_rc_gate_20260518.md`
- `docs/validation/v0.0.3_alpha_rc_gate_20260518.json`
- `release/artifacts/v0.0.3-alpha.manifest.json`
- P2.1-P2.6 validation evidence under `docs/validation/`
- public API manifests under `api/public/`
- storage compatibility manifest and fixtures under `storage/compat/`
- fault matrix at `tests/fault_injection/fault_matrix_v1.json`
- release provenance gate scripts and documentation

The v0.0.3 artifact manifest records:

- `release`: `v0.0.3-alpha`
- `tag`: `v0.0.3-alpha`
- `target_commit`: `be5383d9d555b5b1eac437efc8c8a4201a028cab`
- `slsa_level_claimed`: `null`
- no-go claims include `SLSA L3 completed`, `production-grade supply-chain security`, and `certified provenance`

The manifest `target_commit` intentionally points to the release-content merge commit (`be5383d`). The tag itself points to the later manifest commit (`9bde633`) so the manifest is included in the tag. This split is intentional and not a blocker.

## Safe Claims

- Alpha-grade dual-SDK tamper-evident evidence substrate.
- Restricted canonicalization and canonical text primitives.
- SHA-256 hash-chain primitives.
- Evidence payload schemas and fail-closed validators.
- Read-only verifier predicates with hardened ProofBundle and settlement guardrails.
- Python/TypeScript conformance strengthened by public API, fault-injection, and cross-SDK gates.
- JSONL alpha opt-in storage guardrails and compatibility policy.
- Release artifact manifest and checksum/provenance hygiene gates.
- in-toto/DSSE helpers are deterministic shape helpers only.

## No-go Claims

- Production-ready.
- Compliance-ready.
- Certification system.
- EU AI Act, DORA, or GDPR compliant.
- Full CLI ProofBundle verifier.
- Default signed verification.
- Default anchored verification.
- Runtime governance or AIOS runtime integration.
- Execution authority, scheduler, gateway authority, billing, secret store, or UI read model.
- ACID, database-grade durability, or multi-writer correctness.
- Formal verification or exhaustive mutation testing.
- SLSA L3 completed.
- Certified provenance.
- Production-grade supply-chain security.
- Automatic destructive storage repair.

## Out-of-scope Publish Actions

Not performed:

- GitHub Release creation.
- GitHub Release asset upload.
- PyPI publish.
- TestPyPI publish.
- npm publish.
- npm dist-tag change.
- tag move, retag, tag deletion, or force push.

## GitHub Release Readiness

GitHub Release can be created later from:

- Title: `v0.0.3-alpha`
- Tag: `v0.0.3-alpha`
- Notes file: `docs/validation/v0.0.3_alpha_github_release_draft_20260518.md`
- Required options: `--prerelease`, `--latest=false`

The draft is safe to use as a prerelease body. It should not be used for a latest/stable release.

## PyPI / npm Status

PyPI and npm publishing remain blocked/out-of-scope for this post-tag audit. No package publish commands were run.

## Final Recommendation

`V003AlphaReleaseSurfaceFrozen`.

The tag, branch, manifest, release draft, and evidence surface are aligned for an alpha hardening GitHub prerelease creation step. Do not publish PyPI/npm packages or upload release assets without a separate explicit authorization and a fresh release-asset gate.
