# Issue 75: v1.5.5 Real-Change Boundary

Plan ID: `fe23fa36d63771bf`

## Scope Implemented

- Confirmed the local `v1.5.0..v1.5.5` boundary contains real human work.
- Confirmed the stable release train should publish `v1.5.5` rather than skip.
- Kept release train code, release notes, release-cd policy, tags, registry state, and package metadata unchanged.

## Focused Boundary Evidence

Command:

```bash
git log --no-merges --pretty=tformat:%s v1.5.0..v1.5.5
```

Result:

- 15 non-merge subjects total
- 7 release-prep subjects
- 8 real-work subjects

Real-work subjects:

- `fix: make release planning scripts importable in CI`
- `fix: satisfy markdownlint and plan parser test`
- `Add structured autodev train events`
- `Unify release planning schema and fanout`
- `Unify plan issuance across release tiers`
- `ci: auto-accept major architecture plans`
- `ci: convert accepted plans into task issues`
- `fix(release): skip idle cadence before remote probe`

Command:

```bash
git diff --stat v1.5.0..v1.5.5
```

Result:

```text
39 files changed, 2149 insertions(+), 57 deletions(-)
```

Command:

```bash
git diff --check v1.5.0..v1.5.5
```

Result: exit `0`.

## Publish Decision

The release train should publish `v1.5.5` rather than skip.

Reasoning:

- The boundary contains 8 real-work commits, so it does not satisfy the stable-train idle-cadence skip condition.
- The local release note draft already lists the real boundary commits in `Changes Since Previous Stable`.
- No tag movement, registry movement, or workflow change is needed to reach this decision.

## Release-Note Alignment

`docs/release-notes/v1.5.5.draft.md` already records the non-release-prep commits that should ship in this cut:

- `fix: make release planning scripts importable in CI`
- `fix: satisfy markdownlint and plan parser test`
- `Add structured autodev train events`
- `Unify release planning schema and fanout`

That is sufficient for the boundary confirmation. No release-note edit was required.

## Residual Risk

No residual idle-cadence risk remains in the local evidence. The only broader validation noise is the repo-wide root-to-HEAD hygiene check required by the issue body, which reports historical whitespace and EOF findings outside the `v1.5.0..v1.5.5` decision boundary.
