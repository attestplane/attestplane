# v1.5.7 Real-Change Boundary

Plan ID: `95b8871fa762c209`

## Boundary

The release boundary under review is:

```bash
v1.5.0..v1.5.7
```

## Local Evidence

- `git log --no-merges --pretty=tformat:%s v1.5.0..v1.5.7` returns 23
  non-merge subjects.
- 10 of those subjects match the release-prep regex documented by the stable
  train.
- 13 subjects do not match that regex and therefore count as real work.
- `git diff --stat v1.5.0..v1.5.7` reports:

```text
48 files changed, 2720 insertions(+), 59 deletions(-)
```

- `git diff --check v1.5.0..v1.5.7` is clean.

## Publish Decision

The stable train should publish `v1.5.7` rather than skip. The range contains
real human work, not just `chore(release): prepare ...` commits.

## Policy Cross-Check

Local policy in `docs/runbooks/autodev-train.md` and
`scripts/release/stable_auto_train.py` agrees on the gate:

- skip only when every subject in the range matches the release-prep regex, or
- skip when the range is empty.

The `v1.5.0..v1.5.7` range does neither.

## Release-Note Alignment

`docs/release-notes/v1.5.7.draft.md` already includes the real-work commits
from the boundary, so the release note supports the publish decision without
any tag, registry, or workflow changes.

## Residual Risk

No follow-up idle-cadence issue is required from the local evidence. The
boundary is confirmed as real-work and the skip condition is not met.

## Safety Confirmation

This report does not change tags, publish artifacts, mutate release-cd policy,
or weaken any release gate.
